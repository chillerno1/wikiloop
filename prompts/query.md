# Query the knowledge base

Read `wiki/index.md` first. Then read all wiki pages relevant to this question:

> <QUESTION>

Synthesize an answer grounded entirely in the wiki. Cite every claim with `[Source: page-name]`. If the wiki doesn't contain enough to answer, say so explicitly and suggest what source would fill the gap — do not guess from general knowledge.

If the answer reveals a new connection or insight worth preserving, offer to file it back into `wiki/` as a new page or an update to an existing one. If the answer is substantial and reusable, save it to `outputs/` with a descriptive filename.
