# Execution log

> Live log, written during execution, not after. See `prompts/task-execute.md`.
> Format each entry as `## HH:MM — <step description>` with what was attempted,
> what happened, and the decision or next action. Flag divergences inline with
> `> WIKI GAP:`, `> CONTRADICTS WIKI:`, or `> NEW KNOWLEDGE:`.

## <HH:MM> — starting
<what was attempted>
→ <what happened>
→ <decision>
